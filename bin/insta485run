#!/bin/bash
# insta485run

if [ -e /var/insta485.sqlite3 ]; then
    ./bin/insta485db.sh create
fi

export FLASK_DEBUG=True
export FLASK_APP=insta485
export INSTA485_SETTINGS=config.py
flask run --host 0.0.0.0 --port 8000
